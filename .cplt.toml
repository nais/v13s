# Generated by `cplt init`. Review before trusting.
# Detected: Docker, Go, Environment secrets
# Workspace members:
#   pkg/cli (heuristic scan): Go
#   pkg/api (heuristic scan): Go

[deny]
env = ["DEPENDENCYTRACK_PASSWORD"]

[propose]
# ⚠️  grants access to the Docker socket, effectively root on the host
# Suggested by: (root)
allow_docker = true

[propose.allow]
ports = [4002, 4003, 4004, 4317, 4318, 9090, 9091, 16686]

